# Benchmarks
Criterion benchmarks for selected cuTile Rust kernels and workloads.

- Run `cargo bench -p cutile-benchmarks` to execute the benchmarks.
- Criterion reference: https://bheisler.github.io/criterion.rs/book/criterion_rs.html
